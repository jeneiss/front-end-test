// text input: Store name
// text input: url (with regex to determine if 'myshopify.com' is in the url)

// Email notification sign up (text input)
// checkbox for notification sign up

// Next button is Submit AND a link to <Survey />

// All inputs should have valid values before submit/redirect
// if (submitted) {
//   return <Redirect push to={{
//     pathname: '/result',
//     state: {data: data.toString()}
//   }}
//   />
// }
// https://reactrouter.com/web/api/Redirect/push-bool

import React from 'react'

function StoreInfo() {
  return (
    <h1>store info</h1>
  )
}

export default StoreInfo
